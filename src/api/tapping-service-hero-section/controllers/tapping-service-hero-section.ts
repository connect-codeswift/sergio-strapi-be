/**
 * tapping-service-hero-section controller
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreController('api::tapping-service-hero-section.tapping-service-hero-section');
