/**
 * tapping-service-hero-section router
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter('api::tapping-service-hero-section.tapping-service-hero-section');
