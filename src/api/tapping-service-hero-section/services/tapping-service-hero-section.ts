/**
 * tapping-service-hero-section service
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreService('api::tapping-service-hero-section.tapping-service-hero-section');
